import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  addButton: {
    position: 'fixed',
    bottom: 5,
    right: -10,
  },
}));

export default useStyles;
